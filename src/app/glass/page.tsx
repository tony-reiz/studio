'use client';

// This is a placeholder page.
// The user will provide the actual code.
// I am creating this page so the link on the main page does not 404.

export default function GlassPage() {
  return (
    <div className="flex h-screen w-full items-center justify-center bg-black text-white">
      <p>Page en construction. Le code sera ajouté ici.</p>
    </div>
  );
}
